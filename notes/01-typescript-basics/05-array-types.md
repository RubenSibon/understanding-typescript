# Array types

```ts
// The type of this array is inferred as a string array `string[]`.
const hobbies = ["swimming", "poker", "yoga"];

let explicitTypedArray1: number[];
let explicitTypedArray2: string[];
let explicitTypedArray3: any[] = [1, "two", { name: "Ruben" }];
```
